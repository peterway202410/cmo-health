# 参与贡献

感谢你愿意给「首席代谢官」提问题或改代码。这个项目刻意保持小而本地：不接后端、不上传健康数据。贡献时请沿这条线走。

## 怎么参与

1. 先搜一下 [Issues](https://github.com/peterway202410/cmo-health/issues)，避免重复。
2. Bug 用 Bug 模板，新想法用功能模板。
3. 改代码请先 fork，再从 `main` 拉分支。
4. 提交前在本地跑通：

```bash
npm install
npm test
```

H5 改动建议再跑一次 `npm run dev:h5` 看一眼欢迎页、记录、评估、导入导出。

## 分支与提交

- 分支名：`fix/简短说明` 或 `feat/简短说明`
- 提交说明写清「改了什么、为什么」，用中文或英文都可以
- 一个 PR 只做一件事，方便审

## 代码约定

- 评估、建议、趋势放在 `src/domain/`，保持**纯函数**，不要在这里读存储或发请求
- 读写本地数据走 `src/infra/storage/`
- 备份导入导出走 `src/infra/backup/`
- 不要新增任何把健康数据送到服务器的逻辑
- 文案里不要写成医疗诊断或用药建议

## 测试

领域算法和备份格式请补 `tests/**/*.spec.ts`。  
新增扣分规则、阈值或备份字段时，至少加一条会失败再被修好的用例。

## 许可

本仓库是 GNU GPL v3。你提交的代码默认按同一协议授权。
